version: '3.8'
services:
  postgres:
    image: postgres:16.4
    container_name: weather_postgres_it
    environment:
      POSTGRES_DB: weatherdb
      POSTGRES_USER: testuser
      POSTGRES_PASSWORD: testpass
    ports:
      - "5432:5432"
